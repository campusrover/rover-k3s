apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clouddesktop-ingress
spec:
  rules:
    - host: "vnc.test1.ros.campusrover.org"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: test1-clouddesktop-service
              port:
                number: 80
    - host: "code.test1.ros.campusrover.org"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: test1-clouddesktop-service
              port:
                number: 8080
