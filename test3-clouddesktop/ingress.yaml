apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: clouddesktop-ingress
spec:
  rules:
    - host: "vnc.test3.ros.campusrover.org"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: test3-clouddesktop-service
              port:
                number: 80
    - host: "code.test3.ros.campusrover.org"
      http:
        paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: test3-clouddesktop-service
              port:
                number: 8080
